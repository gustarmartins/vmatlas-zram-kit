name: static-checks

on:
  push:
  pull_request:

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y shellcheck
      - run: ./tests/test-syntax.sh
      - run: shellcheck install.sh uninstall.sh bin/vmatlas-zram libexec/vmatlas-zram-mglru tests/test-syntax.sh
